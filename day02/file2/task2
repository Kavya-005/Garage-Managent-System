#include <iostream>
#include <fcntl.h>
#include <unistd.h>
#include <linux/input.h>
using namespace std;
int main()
{

	while (true)
       	{
		int a = -1,b = -1;
		while(1){
			scanf("%d %d",&a,&b);
			if(a != -1 && b != -1)break;
		}


		
		if (a == 0) {
			if ( b == 0){
				cout<<"volum+" << endl;
			}
			sleep(10);
			//if ( b == 1) {
			cout<<"volum+" << endl;
			//}
		}
	  volum-
		if (a == 1){
			
			if (b == 0){
				cout <<"volum-" << endl;
			}
			sleep(10);
		
			//if ( b == 1){
			cout <<"volum-" << endl;
			//}	
		}
	}
	return 0;
}
